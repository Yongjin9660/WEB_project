extends layout

block content
  each movie in movies
    div(id="movie" + movie._id)
      div.div_img
        img(src=movie.url class='poster')
      hr
      p.title_year
        span= movie.title
        span= '  /  ' + movie.year
      hr
